server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location /api {
        proxy_pass http://server:3001;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
